import LoginClient from "./client"

export const metadata = {
  title: "Sign In - UpSide AI",
  description: "Sign in to your UpSide AI account",
}

export default function LoginPage() {
  return <LoginClient />
}
